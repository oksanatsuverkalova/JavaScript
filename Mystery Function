Доработать игру в загадки:

    a. Избавиться в игре в загадки от дублирования кода, используя массивы и функции.
    b. Сделать так, чтобы программа поддерживала несколько правильных ответов на один и тот же вопрос. Все возможные варианты задаются программистом в коде программы.


<meta charset="utf-8">
<script type="text/javascript">

		var riddles = [
		["Назовёшь её и она исчезнет",
		["Тайна", "ТАЙНА", "тайна"]],

		["Зимой - звезда, весной - вода",
		["снежинка", "Снежинка", "СНЕЖИНКА", "СНЕГ", "снег", "Снег"]],
		
		["Конь стальной, хвост - льняной",
		["игла с ниткой", "Игла с ниткой", "ИГЛА С НИТКОЙ", "иголка с ниткой", "Иголка с ниткой", "ИГОЛКА С НИТКОЙ", "игла с нитью", "Игла с нитью", "ИГЛА С НИТЬЮ"]],
		]


		function gameAnswer(riddle) {
  		return prompt(riddle[0]);
  		}	

		function OkAnswer(riddle, answer) {
  			for (var i = 0; i < riddle[1].length; i++) {
    		if (riddle[1][i] == answer) 	
    			return true;
    	}
  				return false;
		}
  
	var validAnswers = 0;
 
	for (var i = 0; i < riddles.length; i++) {
  
  	var answer = gameAnswer(riddles[i]);
  		if (OkAnswer(riddles[i], answer)) 
  		{
    validAnswers++;
    		alert("Поздравляем, вы угадали!");
  		} else {
    		alert("Вы не угадали!");
  		}
		}
 
alert("Правильных ответов: " + validAnswers);
</script>
